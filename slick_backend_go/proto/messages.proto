syntax = "proto3";
option go_package = "slick_backend_go/proto";
package proto;
service DOMServer {
    // Obtains the feature at a given position.
    rpc ReadDb(ID) returns (Record) {} 
    rpc GetUser(ID) returns (Record) {}
 }

 message ID { 
    string id = 1;
 }

 message Record { 
    string val = 1;
 }